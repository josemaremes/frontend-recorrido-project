/**
 * Retorna el estado del campo deleteDialog.
 * @param {*} state
 * @returns
 */
export function deleteDialog(state) {
  return state.deleteDialog
}

/**
 * Retorna el estado del campo showDialog.
 * @param {*} state
 * @returns
 */
export function showDialog(state) {
  return state.showDialog
}

/**
 * Retorna el estado del campo serviceId.
 * @param {*} state
 * @returns
 */
export function serviceId(state) {
  return state.serviceId
}

/**
 * Retorna el estado del campo serviceList.
 * @param {*} state
 * @returns
 */
export function serviceList(state) {
  return state.serviceList
}
