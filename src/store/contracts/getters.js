/**
 * Retorna el estado del campo contractId
 * @param {*} state
 * @returns
 */
export function contractId(state) {
  return state.contractId
}

/**
 * Retorna el estado del campo contractList.
 * @param {*} state
 * @returns
 */
export function contractList(state) {
  return state.contractList
}

/**
 * Retorna el estado del campo showDialog.
 * @param {*} state
 * @returns
 */
export function showDialog(state) {
  return state.showDialog
}

/**
 * Rertorna el estado del campo deleteDialog.
 * @param {*} state
 * @returns
 */
export function deleteDialog(state) {
  return state.deleteDialog
}
