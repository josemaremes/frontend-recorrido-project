/**
 * Retorna todo el state.
 * @param {*} state
 * @returns
 */
export function wholeState(state) {
  return state
}

/**
 * Retorna el estado del campo contract.
 * @param {*} state
 * @returns
 */
export function contract(state) {
  return state.contract
}

/**
 * Retorna el estado del campo editDialog.
 * @param {*} state
 * @returns
 */
export function editDialog(state) {
  return state.editDialog
}

/**
 * Retorna el estado del campo period.
 * @param {*} state
 * @returns
 */
export function period(state) {
  return state.period
}

/**
 * Retorna el estado del campo service.
 * @param {*} state
 * @returns
 */
export function service(state) {
  return state.service
}

/**
 * Retorna el estado del campo workshiftList.
 * @param {*} state
 * @returns
 */
export function workshiftList(state) {
  return state.workshiftList
}

/**
 * Retorna el estado del campo week.
 * @param {*} state
 * @returns
 */
export function week(state) {
  return state.week
}